import 'package:liquid/liquid.dart';
import 'package:test/test.dart';

void main() {
  group('A group of tests', () {
    // Router router;

    setUp(() {
      // router = Router();
    });

    test('First Test', () {});
  });
}
